import random

lista=[]
def crear_lista():
    for i in range(100):
        n_azar=random.randint(0, 100)
        lista.append(n_azar)
    print(lista)
    return lista  
crear_lista()   
def busqueda_lineal(lista):
    aleatorio=random.randint(0,100)
    for j in range(len(lista)):
        if aleatorio==lista[j]
        print(f"numero aleatorio {aleatorio} = {lista[i]}")

